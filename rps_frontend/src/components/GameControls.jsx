import SoundToggle from './SoundToggle'
export default function GameControls({ phase, onStart, onNext, onReset, soundEnabled, onSoundToggle }) { const busy = !['IDLE', 'RESULT'].includes(phase); return <footer className="controls"><SoundToggle enabled={soundEnabled} onToggle={onSoundToggle} compact /><div><button className="reset" onClick={onReset}>RESET GAME</button><button className="primary" disabled={busy} onClick={phase === 'RESULT' ? onNext : onStart}>{phase === 'RESULT' ? 'NEXT ROUND  →' : busy ? 'ROUND IN PROGRESS' : 'START ROUND  →'}</button></div><span>GESTURE CONTROL ONLY</span></footer> }
