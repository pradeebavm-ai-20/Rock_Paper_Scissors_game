export default function ResultBanner({ game, phase }) { if (!game.result || !['REVEAL', 'RESULT'].includes(phase)) return null; const title = game.result === 'win' ? 'YOU WIN' : game.result === 'lose' ? 'CPU WINS' : 'DRAW'; return <section className={`result-banner ${game.result}`}><div className="result-sparks">✦ ✧ ✦</div><small>ROUND COMPLETE</small><h1>{title}</h1><p>{game.message}</p></section> }
